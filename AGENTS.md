# signal-persona agent notes

Read `~/primary/AGENTS.md` first. This repository is the typed management
contract for talking to the top-level `persona` engine manager; keep daemon
behavior, actors, storage, and text parsing out of this crate.
